#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED

/** \brief Le pidune al usuario que ingrese un operando
 *
 * \return float
 *
 */
float operando();
/** \brief Calcula la suma entre dos numeros
 *
 * \param a float Numero al que quiero sumar
 * \param b float Numero al que quiero sumar
 * \return void
 *
 */
void Sumar(float a,float b);
/** \brief Calcula la resta entre dos numeros
 *
 * \param a float Numero que quiero restar
 * \param b float Numero que quiero restar
 * \return void
 *
 */
void Restar(float a,float b);Calcula la resta

/** \brief Calcula la division entre dos numeros
 *
 * \param a float Nominador
 * \param b float Denominador
 * \return void
 *
 */
void dividir(float a,float b);
/** \brief  Calcula la multiplicacion entre dos numeros
 *
 * \param a float  Numero al cual quiero multiplicar
 * \param b float  Numero al cual quiero multiplicar
 * \return void
 *
 */
void multiplicar(float a,float b);
/** \brief Calcula el factorial de un numero
 *
 * \param a float Numero al cual le quiero calcular su factorial
 * \return void
 *
 */
void factorial(float a);
/** \brief Calcula todas las funciones anteriores
 *
 * \param a float Numero al cual quiero que realice todas las operaciones anteriores
 * \param b float Numero al cual quiero que realice todas las operaciones anteriores
 * \return void
 *
 */
void funciones(float a,float b);


#endif // FUNCIONES_H_INCLUDED
